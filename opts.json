describe('Keystore Wallet Access int main port loock', () => {
    it('can access', () => {
        cy.visit('/access/keystore')
        let fixture = 'keystore_v5.json'
        cy.get('.file_input input').attachFile(fixture)
    })
})
